export {};

// import { Knex } from 'knex';

// export async function test(db: Knex): Promise<void> {
//   try {
//     const rows = await db.select('CollectionName').from<SpecCollection>('collection');
//     if (rows.length == 0) {
//       throw Error('No collections found');
//     }
//   } catch (err) {
//     await this.clear();
//     throw err;
//   }
// }
